#!/bin/sh

# Check if disk was passed over
if [ -z "$1" ] ; then
  echo "ERROR: You have to pass over a disk"
  exit 1
fi

# Import MOS functions
. /usr/local/lib/functions/mos-common

# Set HEADER if GitHub Token was specified
GH_TOKEN=$(jq -r '.github // empty' /boot/config/system/tokens.json 2>/dev/null)
if [ ! -z "$GH_TOKEN" ] ; then
  TOKEN="$(decrypt_password "$GH_TOKEN" "tokens")"
  HEADER=(--header "Authorization: Bearer $TOKEN")
else
  HEADER=()
fi

# Set Architecture
if [ "$(get_arch)" == "amd64" ] ; then
  ARCHITECTURE="x86_64"
  DEB_ARCH="amd64"
elif [ "$(get_arch)" == "arm64" ] ; then
  ARCHITECTURE="arm64"
  DEB_ARCH="arm64"
fi

# Clean Device path
DISK="${1#/dev/}"
DISK="/dev/$DISK"

# Check if partition is specified in path
case $DISK in
  /dev/sd[a-z]|/dev/vd[a-z]|/dev/nvme[0-9]*n[0-9]*|/dev/mmcblk[0-9]*)
  ;;
  /dev/sd[a-z][0-9]*|/dev/vd[a-z][0-9]*|/dev/nvme[0-9]*n[0-9]*p[0-9]*|/dev/mmcblk[0-9]*p[0-9]*)
    echo "ERROR: Please specify a whole disk without the Parititon"
    exit 1
  ;;
  *)
    echo "ERROR: Unknown disk specified"
    exit 1
  ;;
esac

# Check if device actually exists
if [ ! -e "$DISK" ] ; then
  echo "ERROR: Disk $DISK not found"
  exit 1
fi

# Check if device is mounted
if lsblk -no MOUNTPOINT $DISK | xargs | grep -qv "^$" ; then
  echo "ERROR: $DISK seems to be mounted, not installing to mounted disk"
  exit 1
fi

# Check if filesystem type was passed over, if
# not default to xfs
if [ -z "$2" ] ; then
  echo "No filesystem type defined, defaulting to ext4"
  FS_TYPE="ext4"
else
  FS_TYPE="$2"
fi

# Check if a valid filesystem type was passed over
case $FS_TYPE in
  vfat|ext4|btrfs|xfs)
  ;;
  *)
    echo "ERROR: $FS_TYPE is not a supported filesystem type"
    echo "       Supported filesystems are:"
    echo "       - vfat (not recommended for HDD/SSD/NVME boot)"
    echo "       - ext4"
    echo "       - btrfs"
    echo "       - xfs"
    exit 1
  ;;
esac

if [ "$4" = "true" ] ; then
  if [ "$FS_TYPE" = "vfat" ] ; then
    PARTITIONS="one"
    EXTRA_MESSAGE=""
    echo "WARNING: Extra partions on vfat boot devices are not supported"
  else
    PARTITIONS="three"
    EXTRA_MESSAGE="2 with 16GB as the data partition in $FS_TYPE\n- Partition 3 with the remaining free space as an extra partition in $FS_TYPE"
  fi
else
  PARTITIONS="two"
  EXTRA_MESSAGE="2 with the remaining free space in $FS_TYPE as the data partition"
fi

# Ask user to continue except when quiet is passed over
if [ "$3" != "quiet" ] ; then
  if [ "$FS_TYPE" = "vfat" ] ; then
    MESSAGE="This will format the whole device $DISK in fat32\n\nATTENTION: Formatting a whole device other than a USB Flash Drive is discuraged\nIf you are using whole disk then maybe consider other filesystem types like:\n  - ext4\n  - btrfs\n  - xfs"
  elif [ "$FS_TYPE" = "xfs" ] ; then
    MESSAGE="This will format the device $DISK with $PARTITIONS partitions:\n- Partition 1 with 64MB in fat32 for the ESP partition to allow EFI boot\n- Partition $EXTRA_MESSAGE\n\nATTENTION: Formatting in xfs will probably cause issues with GRUB displaying\n           error messages and other various unwanted behaviour"
  else
    MESSAGE="This will format the device $DISK with $PARTITIONS partitions:\n- Partition 1 with 64MB in fat32 for the ESP partition to allow EFI boot\n- Partition $EXTRA_MESSAGE"
  fi
  echo
  echo -e "$MESSAGE"
  echo

  echo -n "Proceeed? [y/n] "
  read answer

  case "$answer" in
    [Yy]|[Yy][Ee][Ss])
    ;;
    *)
      echo "Abort"
      exit 1
    ;;
  esac
fi

[ "$5" == "api" ] && notify -t "MOS Installation" -m "Installation started, please wait" -p "normal"

# Create directories
mkdir -p /var/mos/installer/esp /var/mos/installer/boot

# Set LABEL
LABEL="MOS"

# If it's a NVME or MMC add p for partitions
case $DISK in
  /dev/nvme[0-9]*n[0-9]*|/dev/mmcblk[0-9]*)
    PARTITION="p"
  ;;
  *)
    PARTITION=""
  ;;
esac

# Refresh device symlinks with udev
udevadm_refresh() {
  udevadm trigger --subsystem-match=block
  udevadm settle
}

# Umount directories
umount_directories() {
  umount /var/mos/installer/boot /var/mos/installer/esp 2>/dev/null
}

# Define download function
download_asset() {
  echo "Downloading GRUB${2}"
  curl --progress-bar -L \
  "${HEADER[@]}" \
  --header "Accept: application/octet-stream" \
  --header "X-GitHub-Api-Version: 2022-11-28" \
  --output "/var/mos/installer/grub.deb${2}" \
  "https://api.github.com/repos/mos-nas/mos-grub/releases/assets/$1"

  if [ "$2" = ".md5" ]; then
    echo "Checking md5"
    if [ "$(md5sum /var/mos/installer/grub.deb | awk '{print $1}')" = "$(cat /var/mos/installer/grub.deb${2} | awk '{print $1}')" ] ; then
      echo "md5 OK"
    else
      echo "md5 ERROR"
      [ "$3" == "api" ] && notify -t "MOS Installation" -m "Error downloading GRUB, md5 check failed" -p "alert"
      exit 1
    fi
  fi
}

# Get GRUB JSON and assets
GRUB_JSON=$(curl -s --request GET \
  --url "https://api.github.com/repos/mos-nas/mos-grub/releases/latest" \
  "${HEADER[@]}" \
  --header "X-GitHub-Api-Version: 2022-11-28")

GRUB_DEB=$(echo "$GRUB_JSON" | jq -r --arg arch "$DEB_ARCH" '.assets[] | select(.name | startswith("mos-grub") and endswith($arch + ".deb")) | .id')
GRUB_DEB_MD5=$(echo "$GRUB_JSON" | jq -r --arg arch "$DEB_ARCH" '.assets[] | select(.name | startswith("mos-grub") and endswith($arch + ".deb.md5")) | .id')

# Download GRUB from mos-grub repo
download_asset "$GRUB_DEB" "" "$5"
download_asset "$GRUB_DEB_MD5" ".md5" "$5"

# Install GRUB
echo "Installing GRUB"
if ! dpkg --install /var/mos/installer/grub.deb >/dev/null ; then
  echo "ERROR: GRUB installation failed"
  [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error GRUB installation failed" -p "alert"
  exit 1
fi

# Format disk depending which filesystem is passed over
if [ "$FS_TYPE" != "vfat" ] ; then
  # Create partition table and create partitions
  echo "Wiping old Partitions and creating new Partitions"
  wipefs -a $DISK
  parted -s $DISK mklabel gpt
  parted -s $DISK mkpart ESP fat32 1MiB 65MiB
  parted -s $DISK set 1 boot on

  # Check if 3rd partition is wanted and partition disk
  if [ "$PARTITIONS" = "three" ] ; then
    parted -s $DISK mkpart primary $FS_TYPE 65MiB 16449MiB
    parted -s $DISK mkpart primary $FS_TYPE 16449MiB 100%
  else
    parted -s $DISK mkpart primary $FS_TYPE 65MiB 100%
  fi

  # Refresh symlinks with udev
  udevadm_refresh

  # Format ESP partition
  echo "Formatting ESP Partition with FAT32"
  if ! mkfs.fat -F32 -c -I ${DISK}${PARTITION}1 ; then
    echo "ERROR: Format from ESP Partition failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error formatting ESP Partition" -p "alert"
    exit 1
  fi
  # Set Force flag
  if [ "$FS_TYPE" = "ext4" ] ; then
    FORCE_OPT="-F"
  else
    FORCE_OPT="-f"
  fi
  # Set additinal flags
  if [ "$FS_TYPE" = "xfs" ] ; then
    OPT_FLAGS=" -n ftype=0 -m crc=0"
  else
    OPT_FLAGS=""
  fi
  # Format boot partition depending if 3rd partition is wanted
  if [ "$PARTITIONS" = "three" ] ; then
    echo "Formatting boot Partition with $FS_TYPE"
    if ! mkfs.${FS_TYPE} ${FORCE_OPT}${OPT_FLAGS} -L "$LABEL" ${DISK}${PARTITION}2 ; then
      echo "ERROR: Format from Boot Partition failed"
      [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error formatting Boot Partition" -p "alert"
      exit 1
    fi
    echo "Formatting extra Partition with $FS_TYPE"
    if ! mkfs.${FS_TYPE} ${FORCE_OPT}${OPT_FLAGS} ${DISK}${PARTITION}3 ; then
      echo "ERROR: Format from extra Partition failed"
      [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error formatting extra Partition" -p "alert"
      exit 1
    fi
  else
    echo "Formatting boot Partition with $FS_TYPE"
    if ! mkfs.${FS_TYPE} ${FORCE_OPT}${OPT_FLAGS} -L "$LABEL" ${DISK}${PARTITION}2 ; then
      echo "ERROR: Format from Boot Partition failed"
      [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error formatting Boot Partition" -p "alert"
      exit 1
    fi
  fi

  # Mount partitions
  echo "Mounting Partitions"
  if ! mount ${DISK}${PARTITION}1 /var/mos/installer/esp ; then
    echo "ERROR: Mount from ESP Partition failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error mounting ESP Partition" -p "alert"
    umount_directories
    exit 1
  fi
  if ! mount ${DISK}${PARTITION}2 /var/mos/installer/boot ; then
    echo "ERROR: Mount from Boot Partition failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error mounting Boot Partition" -p "alert"
    umount_directories
    exit 1
  fi

  # Copy files from current boot device or from tar archive
  if [ ! -z "$6" ] ; then
    echo "Copying files from archive"
    tar -C /var/mos/installer/boot/ --strip-components=1 -xf "$6"
  else
    echo "Copying files from boot device"
    cp -R /boot/* /var/mos/installer/boot/
  fi

  # Remove unclean shutdown file if it's there
  if [ -f /var/mos/installer/boot/config/system/mos.chk ] ; then
    rm -f /var/mos/installer/boot/config/system/mos.chk
  fi

  # Remove EFI directory since not needed
  rm -rf /var/mos/installer/boot/EFI

  # Install GRUB bootloader
  echo "Installing GRUB"
  if ! grub-install --target=${ARCHITECTURE}-efi --efi-directory=/var/mos/installer/esp --boot-directory=/var/mos/installer/boot --removable --recheck --force ; then
    echo "ERROR: Installing GRUB failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error installing GRUB Bootloader" -p "alert"
    exit 1
  fi
else
  # Create partition table and create partitions
  echo "Wiping old Paritions and creating Partition"
  wipefs -a $DISK
  parted -s $DISK mklabel gpt
  parted -s $DISK mkpart ESP fat32 1MiB 100%
  parted -s $DISK set 1 boot on

  # Refresh symlinks with udev
  udevadm_refresh

  # Format partition
  echo "Formatting Partition with FAT32"
  if ! mkfs.fat -F32 -c -I ${DISK}${PARTITION}1 ; then
    echo "ERROR: Formatting failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error formatting failed" -p "alert"
    exit 1
  fi

  # Mount partition
  echo "Mounting Partition"
  if ! mount ${DISK}${PARTITION}1 /var/mos/installer/boot ; then
    echo "ERROR: Mount from Partition failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error mounting Partition" -p "alert"
    exit 1
  fi

  # Copy files from current boot device or from tar archive
  if [ ! -z "$6" ] ; then
    echo "Copying files from archive"
    tar -C /var/mos/installer/boot/ --strip-components=1 -xf "$6"
  else
    echo "Copying files from boot device"
    cp -R /boot/* /var/mos/installer/boot/
  fi

  # Remove unclean shutdown file if it's there
  if [ -f /var/mos/installer/boot/config/system/mos.chk ] ; then
    rm -f /var/mos/installer/boot/config/system/mos.chk
  fi

  # Install GRUB bootloader
  echo "Installing GRUB"
  if ! grub-install --target=${ARCHITECTURE}-efi --efi-directory=/var/mos/installer/boot --boot-directory=/var/mos/installer/boot --removable --recheck --force ; then
    echo "ERROR: Installing GRUB failed"
    [ "$5" == "api" ] && notify -t "MOS Installation" -m "Error installing GRUB Bootloader" -p "alert"
    exit 1
  fi
fi
# Sync and umount
echo "Syncinc Filesystems"
sync
echo "Unmounting Parititons"
umount_directories

echo "Done!"
echo
echo "Please either unplug your old USB Boot device to boot form"
echo "the new boot device or change the Boot order in your BIOS."
echo "Please reboot..."

[ "$5" == "api" ] && notify -t "MOS Installation" -m "Installation done! Please shutdown your server, remove your old boot device and change the boot order in your BIOS if needed." -p "normal"
