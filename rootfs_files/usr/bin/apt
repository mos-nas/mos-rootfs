#!/bin/bash
COMMAND=$(basename "$0")

echo "WARNING: USAGE of $COMMAND in MOS is discouraged"
echo -n "Continue? [y/N]: "
read -r response
case "$response" in
  [yY][eE][sS]|[yY]) 
    exec /usr/bin/$COMMAND.orig "$@"
  ;;
  *) 
    exit 1
  ;;
esac
